# For development purpose, we point to a test repo
# to avoid polluting the official repo with PR of tests
# file only loaded in development mode
VITE_ORGANIZATION=AurelienC
VITE_REPO_NAME=transport-base-nationale-covoiturage-test
VITE_BRANCH_NAME=main

# You need the-nice-bot secret key which is stored in Netlify as
# an environment variable.
VITE_THE_NICE_BOT_SPEC=put-the-nice-bot-secret-key-here